# Symbol Set Management — Product Stage 9 Implementation Plan

> **For Hermes / OpenClaw orchestration and Claude Code:** this is the one committed, controlling plan for Product Stage 9. Ephemeral `/tmp` manifests, restart prompts, and review verdicts produced by either orchestration layer are session-scoped evidence, not sources of truth — reconcile against this file and the current repository state (`git log`, `git status`, Alembic head) before resuming or dispatching a Stage 9 work package. If a `/tmp` artifact and this file disagree, this file wins; update this file rather than trusting a stale scratch manifest.

**Status:** IN PROGRESS — WP9.1 (schema) is done in the working tree but not yet committed (see §2's implementation note); WP9.2-9.9 have not started. This document records the baseline evidence, the finalized work-package sequence, and the nine product decisions Chris confirmed this session (§4 — unusually long for this stage, since Stage 9's spec text left more open than Stage 5-8 did, and this stage touches privacy/PII and anti-gaming controls that would have been risky to guess). Per the Stage 5-8 convention, no further work package may start without its own separate explicit go-ahead.

**Controlling product sources:**

- Programme plan: `docs/2026-08-10-symbol-set-management-implementation-plan.md`, §15 ("Stage 9 — usage telemetry and contribution reputation," lines 907-939) — the controlling scope statement. Dependency graph (`:452`): Stage 8 (done) → Stage 9 → Stage 10 (agent oversight, not yet planned).
- `docs/Symbol Set Management Spec v0.3.md` — §8 entity table (`ContributionEvent`, line 411), §12 ("Contribution and gamification," lines 609-654: scoring model, badges, anti-gaming), §13 ("Analytics and telemetry," lines 655-682: required measures, event semantics, privacy/attribution), AC-22 (line 805), API surface (`POST /api/v1/catalog/usage`, `GET /api/v1/organizations/{orgId}/contributions`, lines 936/943), Appendix B ("Initial Event Catalog," lines 966-995).
- `docs/plans/2026-08-08-symbol-set-management-decision-addendum.md` — I-13 (usage-event domain separation from `CatalogApiUsageEvent`/audit/security/reputation), I-14 (v1 reputation scope: auditable counts and badges only, no weighted points/commercial benefit), I-22 (Stage-10-facing finding-vocabulary freeze, cited here as the precedent Stage 9's own event vocabulary should mirror).
- `docs/Symbol Set Management Spec v0.3.md` O1 (line 839): commercial benefits for reputation are deferred until quality/economics/anti-gaming controls are validated — matches I-14.
- `docs/plans/2026-09-03-symbol-set-management-stage8-implementation-plan.md` — plan-format precedent (this file mirrors its structure exactly) and Stage 8's closing state, which this stage extends without regressing.

**Authority note:** this file is documentation only. It does not itself authorize a commit, push, real/shared database migration, deployment, service restart, feature activation, or destructive cleanup. Each work package still requires its own explicit go-ahead before code changes.

---

## 1. Repository baseline and current-state evidence

Baseline captured 2026-09-03, at the start of this planning session:

- Repository: `/docker/openclaw-hz0t/data/symgov`. Branch `main`, `HEAD` = `4ae4529` ("fix: close Stage 8 with WP8.7 audit fixes and closing report"). Local `main` is 4 commits ahead of `origin/main`; nothing has been pushed. Working tree clean.
- Alembic sole head: `20260902_0037` — the exact head Stage 8 left behind. No migration has landed since; Stage 9 is fully greenfield on schema.
- Stage 8 (WP8.1-8.7) is complete: organization-aware Catalog list/detail/favourites, Catalog scope/status badges, and a whole-stage audit that found and fixed a stale-flag visibility gap. Stage 8's own plan doc contains no forward reference into Stage 9's scope beyond the programme plan's dependency graph.

### 1.1 No usage-telemetry or reputation infrastructure exists today — fully greenfield

Grepped migrations and `backend/symgov_backend/models/schema.py` for `usage`, `telemetry`, `analytics`, `event_type`, `ContributionEvent`, `badge`: nothing matching Stage 9's own scope exists. Two adjacent-but-distinct tables already exist and must **not** be repurposed, per I-13's explicit instruction and the spec's own §13.3 ("Separate operational logs, product analytics, security signals and contribution reputation"):

- `CatalogApiUsageEvent` (`schema.py:570-598`, migration `20260710_0019_catalog_api_usage_events.py`) — API-key traffic only (`routes/catalog.py`'s surface). Fields: `api_key_id`, `customer_name_snapshot`, `integration_name_snapshot`, `scope_used`, `method`, `path`, `route_name`, `status_code`, `latency_ms`, `request_id`, `query_text`, `symbol_ref`, `result_count`, `ed_query_type`, `user_agent`, `client_ip_hash`, `application_name`, `application_version`, `created_at`. I-13: "Keep `CatalogApiUsageEvent` for API-key traffic. Add a separate server-derived authenticated browser/product-usage event domain."
- `LLMUsageEvent` (`schema.py:601+`, migration `20260730_0025_llm_usage_events.py`) — LLM call cost/latency tracking, structurally unrelated to product usage but the **best structural precedent to imitate**: a frozen, `CheckConstraint`-enforced enum vocabulary for every categorical column (`environment`, `use_case`, `service_name`, `provider`, `status`, `initiator_kind`, etc.), covering indexes on `(dimension, occurred_at)` pairs, a JSONB escape-hatch column (`other_usage_json`) with its own `jsonb_typeof(...) = 'object'` constraint, and cross-column provenance constraints (e.g. `llm_usage_events_cost_provenance`). This is the pattern Stage 9's new product-usage event table should follow, not `AuditEvent`'s looser shape.
- `AuditEvent` (`schema.py:796-805`) — generic entity-mutation audit log: `id, entity_type, entity_id, action, actor_id (nullable FK users), payload_json (JSONB), created_at`. Used across ~15 files for governance mutations (demotion, promotion, review decisions, etc.). This is *audit*, not *analytics* — I-13 and spec §13.3 both require these stay separate domains. Stage 9 must not repurpose `AuditEvent` rows for browse/search/preview volume, and must not stop emitting `AuditEvent` rows for governance mutations that already emit them — the new product-usage event table is additive, emitted *alongside* the existing audit trail for governance events, and standalone for browse-only events that have no `AuditEvent` counterpart today (preview, download, search, Favorite, context/set selection).

### 1.2 Spec's required event catalog is large; the scoring/reputation model is explicitly NOT all v1 scope

Appendix B (spec lines 966-995) lists ~25 canonical event names spanning session start, organization/project/set lifecycle, context resolution, preview/download/Favorite, private-draft/review/promotion/demotion, and agent-finding lifecycle (the last of these is Stage 10's own domain, not Stage 9's — Stage 9 should emit `contribution_awarded`/`contribution_reversed` and the governance-adjacent events already relevant to reputation, but agent-finding events belong to whichever stage actually builds `AgentFinding`/`AgentConfiguration`, i.e. Stage 10, per the programme plan's own dependency ordering).

**I-14 is the load-bearing scope boundary for reputation:** *"Initial reputation consists of auditable contribution/review counts and badges. Weighted points and commercial benefits require a later versioned policy accepted by Chris. Reputation never grants authority."* This directly narrows spec §12.1's illustrative points table (+100/+40/+20/+5/+25) — that table is not itself an accepted v1 scoring policy; it is the spec's own "illustrative" placeholder (§12.1's column header literally reads "Illustrative points"), and O1/I-14 both defer the real policy. **Read literally, this means Stage 9 v1 should build the append-only `ContributionEvent` ledger and count-based badges, but should not activate a live weighted-points display or leaderboard ranking derived from those points**, until Chris accepts a versioned scoring policy separately. §4 Q2 below asks Chris to confirm this reading before any schema/service work begins, since it's the single decision most likely to reshape `ContributionEvent`'s own column set (whether `points` is even populated in v1, or reserved-but-unused pending the later policy).

### 1.3 "Server-derived" and "same transaction" are the load-bearing integrity requirements

Programme plan line 916: *"Derive principal/context server-side and emit in the same transaction for governance mutations; low-value browse events may use a bounded durable ingestion path with documented loss behavior."* This is two different reliability tiers, not one:

- **Governance-mutation events** (organization review decision, promotion, demotion, role/capability change, project/set create-update-archive, icon generated/uploaded/removed) must be emitted transactionally, in the same database transaction as the mutation itself — mirroring how `AuditEvent` rows are already added inline within `session.add(...)` calls throughout the governance service modules (Stage 5-8 precedent, ~15 existing call sites). No new queue/worker infrastructure is needed for this tier; it is a straightforward additive `session.add(ProductUsageEvent(...))` alongside each existing `session.add(AuditEvent(...))` call, or at each mutation's own commit point where no `AuditEvent` exists yet (e.g. project/set lifecycle, which today has no audit trail at all — confirmed by the same grep, a genuine gap this stage would also close incidentally).
- **Low-value browse events** (preview, search, zero-result search, context resolution, symbol download-without-mutation) explicitly may use "a bounded durable ingestion path with documented loss behavior" — i.e., best-effort, not transactionally guaranteed. This is architecturally significant: it is the one place in this stage where a genuinely new piece of infrastructure (an async/buffered ingestion path) is plausible, rather than a straightforward synchronous insert. **This repository has no existing async task queue or background worker infrastructure for anything resembling this** (confirmed: no Celery/RQ/arq/similar dependency in `backend/requirations.txt`, no background-worker process alongside the FastAPI app) — introducing one would be a materially larger architectural decision than anything Stage 5-8 needed, and is exactly the kind of choice that needs Chris's explicit sign-off before any code is written (§4 Q5).

### 1.4 No dashboard-building precedent exists on the frontend; no chart library is installed

`OrganizationAdminPage.js` and `PlatformAdminPage.js` (grepped, not fully read in this pass) use plain React (`createElement`, hooks) with hand-rolled stat blocks (`dt`/`dd` pairs) — no chart/visualization library. Confirmed via `package.json`: no `recharts`, `d3`, `victory`, `chart.js`, or similar dependency exists anywhere in `frontend/`. Any Stage 9 dashboard UI is genuinely greenfield on both axes: the data-fetching pattern (existing admin pages call REST endpoints directly with hooks — a pattern to follow, not a gap) and any chart rendering (nothing to reuse; a real choice between hand-rolled stat tiles matching the existing visual language, per `CLAUDE.md`'s "prefer the existing visual language," versus introducing a new charting dependency, which `CLAUDE.md`'s validation section requires Chris's separate explicit approval for as a "new npm dependency"). §4 Q6 asks this directly rather than assuming either answer.

### 1.5 Retention periods and aggregation thresholds are unspecified numbers, not just unspecified policy

Spec §13.3: *"Support retention policies and aggregate reporting thresholds."* This is a requirement to have a documented, enforced number, not a design pattern to implement generically — and no number appears anywhere in the spec, decision addendum, or `CLAUDE.md`. Per `CLAUDE.md`'s "do not invent production metrics, workflow states, or backend contracts," Stage 9 must not invent a retention period (e.g. "90 days") or a minimum-aggregation threshold (e.g. "suppress any aggregate cell representing fewer than 5 distinct users") without Chris confirming the actual number — these are real business/privacy decisions, not implementation details. §4 Q7.

### 1.6 Anti-gaming rules are qualitative; "flag suspicious linked accounts" has no defined detection rule

Spec §12.4 lists five anti-gaming principles (no credit for raw uploads/likes/self-downloads/same-org-only activity, dedupe before review, hide points from reviewers, append-only + reversal corrections, rate-limit + flag suspicious linked accounts). The first four translate directly into concrete, testable rules on the `ContributionEvent` ledger and its awarding service. The fifth ("flag suspicious linked accounts or organizations without automatic punishment," per the programme plan's own added qualifier) has no defined detection heuristic anywhere in the source documents — this is real design work, not a lookup, and risks inventing a fabricated "fraud detection" mechanism if not scoped precisely. §4 Q8.

### 1.7 Existing pattern for "separate domain, frozen vocabulary" tables to imitate structurally

`LLMUsageEvent`'s own `__table_args__` (schema.py:605-634) is the concrete template for how this repository already expresses "an append-only event table with a frozen, schema-validated categorical vocabulary" — every enum-shaped column gets its own named `CheckConstraint`, cross-column provenance rules get their own constraint, and a JSONB escape hatch (`other_usage_json`) is itself constrained to `jsonb_typeof(...) = 'object'` rather than left as an unconstrained free-form blob. Stage 9's new product-usage event table (and `ContributionEvent`) should mirror this exactly: a single `event_type` column constrained to the frozen Appendix-B-derived vocabulary (minus Stage 10's agent-finding events, per §1.2), rather than a free-text column.

---

## 2. Work-package sequence (finalized against §4's confirmed decisions)

1. **WP9.1 — Schema: product-usage event table. DONE (2026-09-04), not yet committed.** New `ProductUsageEvent` table, additive migration, `LLMUsageEvent`-style frozen `CheckConstraint` vocabulary for `event_type` scoped to the **browse-facing core subset only** (session start, context resolution, preview, download, Favorite change, set selection — §4 Q1), nullable session-mode/organization/project/set/symbol-revision/format dimensions (I-13: "nullable future scope dimensions only where useful"), no raw search text by default (I-13), pseudonymous/internal user identifier only (spec §13.3). Includes the 90-day raw-row retention/purge mechanism (§4 Q7). No emission wiring yet — schema only, so this package can be reviewed and its constraints validated before any endpoint starts writing to it. See the implementation note below.
2. **WP9.2 — Governance-mutation event emission (transactional tier) + project/set audit-gap closure.** Extends WP9.1's `CheckConstraint` vocabulary additively with the governance-lifecycle event names actually needed here (organization review decision, promotion, demotion, project/set create/update/archive/select, icon generated/uploaded/removed, role/capability changes — §4 Q1's deferred half). Adds `session.add(ProductUsageEvent(...))` alongside each mutation point, same transaction, mirroring the existing `AuditEvent` pattern (§1.3). Per §4 Q4, also adds a new `AuditEvent` row for project/set lifecycle actions specifically (today's genuine gap), alongside the new product-usage event, in the same transaction. Depends on WP9.1.
3. **WP9.3 — Browse-event ingestion (synchronous tier).** The `POST /api/v1/catalog/usage` endpoint (spec line 936) for preview/download/Favorite/context/set-switching events. Per §4 Q5, inserts synchronously on the request thread — no new async/worker infrastructure. Depends on WP9.1.
4. **WP9.4 — Aggregation services and admin dashboard APIs.** Organization Admin and Platform Admin aggregate-read endpoints (`GET /api/v1/organizations/{orgId}/...` usage summaries), default-to-aggregate-counts, raw per-user visibility restricted per spec line 917. Per §4 Q7, enforces the 3-distinct-user minimum aggregation threshold (suppress/group any smaller cell) and keeps aggregate rollups indefinitely even after raw rows are purged at 90 days. Depends on WP9.1-9.3 for real data to aggregate.
5. **WP9.5 — Contribution/reputation read model.** `ContributionEvent` ledger (append-only + reversal-record correction, per §12.4), `GET /api/v1/organizations/{orgId}/contributions` (spec line 943). Per §4 Q2, **no `points` column** — counts and badges only. Per §4 Q3, computes exactly three badges: First Contribution, Contributor Organization, Community Partner (each with an unambiguous trigger); Multi-Discipline Contributor and Metadata Improver are explicitly deferred pending Chris defining their thresholds. Depends on WP9.1's table-design precedent but is a structurally separate table/service, not built on top of WP9.2-9.4.
6. **WP9.6 — Anti-gaming enforcement.** Per §4 Q8, builds four of the five §12.4 rules: no credit for uploads/likes/self-downloads/same-org-only activity; dedupe before review; hide reputation/contribution detail from reviewers during review (the "hide points" rule, reduced to this scope given Q2's no-points decision); append-only ledger with reversal-only corrections; rate-limit repeated submissions. Does **not** build "flag suspicious linked accounts" — no Chris-confirmed detection signal exists for it. Depends on WP9.5.
7. **WP9.7 — Frontend: usage dashboards.** Organization Admin and Platform Admin dashboard UI. Per §4 Q6, hand-rolled stat tiles matching `OrganizationAdminPage.js`'s existing `dt`/`dd` convention — no new charting dependency. Depends on WP9.4.
8. **WP9.8 — Frontend: contribution/reputation UI.** Individual-user private stats, organization-only public attribution (§12.2). Per §4 Q9, **no leaderboard** in this stage — recorded as an explicit, deferred nice-to-have. Depends on WP9.5-9.6.
9. **WP9.9 — Whole-stage audit.** Mirrors WP5.6/WP6.6/WP7.7/WP8.7 exactly: full regression suite, Contract Review, Security Review (with particular attention to the PII/privacy boundary §13.3 sets and to the confirmed 90-day retention/3-user aggregation-threshold enforcement from §4 Q7 — this stage handles individual user behavioral data in a way Stage 5-8 did not). Depends on WP9.1-9.8 all committed.

**WP9.1 implementation note (2026-09-04):**

- `backend/symgov_backend/models/schema.py` gained `ProductUsageEvent` (table `product_usage_events`), added to `models/__init__.py`'s exports. `event_type` is a `CheckConstraint` enum of exactly the seven browse-facing core event names (§4 Q1): `personal_session_started`, `organization_selected`, `context_resolved`, `set_selected`, `symbol_previewed`, `symbol_downloaded`, `favorite_changed` — governance-lifecycle event types are deliberately absent, to be added additively by WP9.2.
- Columns: `id`, `event_type`, `occurred_at`, `session_mode`, `user_id` (FK `users.id`, not null — spec §13.3's "internal ID" requirement is satisfied by using the existing internal UUID directly, no separate pseudonymization scheme invented), nullable `organization_id`/`project_id`/`symbol_set_id`/`governed_symbol_id`/`symbol_revision_id` dimensions (I-13's "nullable future scope dimensions only where useful"), nullable `symbol_source` (reuses Stage 8's exact `public`/`organization_private` vocabulary), nullable `format`, `favourite_action`, `context_resolution_basis`.
- Cross-column `CheckConstraint`s enforce that `format` is present if and only if `event_type = 'symbol_downloaded'`, `favourite_action` if and only if `event_type = 'favorite_changed'`, `context_resolution_basis` if and only if `event_type` is `context_resolved`/`set_selected`, and `organization_id` is present if and only if `session_mode = 'organization'` (mirroring `user_sessions`' own existing `ck_user_sessions_mode_active_org` invariant). `context_resolution_basis`'s own allowed values (`explicit`, `user_preference`, `project_default`, `organization_default`, `none`) are copied verbatim from `symbol_context_service.py`'s `_resolved_set`'s real, already-shipped `reason` vocabulary — not invented.
- Modeled on `LLMUsageEvent`'s own frozen-`CheckConstraint`-vocabulary style (Stage 9 plan §1.7), but deliberately diverges from its fully append-only trigger: rows are immutable via an `UPDATE`-blocking trigger only (`trg_prevent_product_usage_events_update`), not `UPDATE OR DELETE` — `DELETE` remains permitted at the database level because the confirmed 90-day retention purge (§4 Q7) depends on being able to delete expired rows. Migration also grants `SELECT, INSERT, DELETE` (no `UPDATE`) on the table to `symgov_app`, baked in alongside the table's own creation, matching the `organizations`/`projects` migrations' own precedent of granting at creation time rather than leaving it to be granted ad hoc by whichever test or endpoint touches it first.
- `backend/symgov_backend/product_usage_retention.py` (new): `purge_expired_product_usage_events(session, *, now=None)` deletes rows with `occurred_at` older than the confirmed 90-day window and returns the count deleted; caller commits. Deliberately not wired to any scheduler/cron — invoking this on a recurring cadence in a live environment is a deployment/operations decision requiring its own separate approval, per this repository's prohibition on live mutations without it. This is a tested, callable unit only, exactly matching this package's own "schema (and its supporting retention mechanism) only, no emission wiring yet" scope.
- Migration: `backend/alembic/versions/20260904_0038_product_usage_events.py`, `down_revision = "20260902_0037"` (Stage 8's final head).
- Tests: `tests/test_wp91_product_usage_events_postgresql.py` (14 cases, real Postgres + real HTTP-free ORM inserts, since this package has no endpoint yet): all seven core event types insert successfully with their correct cross-column fields; an unknown `event_type` is rejected; each of the four cross-column constraints (format-on-download, favourite-action-on-favorite-changed, context-basis-on-context-events, organization-id-on-organization-mode) is proven both ways (required value present when it must be, rejected when it mustn't be present); bad `session_mode`/`symbol_source` enum values are rejected; rows are immutable once inserted (`UPDATE` raises); `DELETE` remains permitted and `purge_expired_product_usage_events` deletes only rows older than the 90-day window, leaving newer rows untouched.
- Full portable backend suite: 2174 passed, 6 pre-existing failures (independently reproduced, identical names to every Stage 8 note — confirmed unrelated to this package), up from Stage 8's final 2160 by exactly this package's own 14 new tests, no regressions. Two additional test files (`test_public_projection_migration.py`, `test_wp56_defense_in_depth_hardening.py`) each carry a documented "update this constant whenever a later migration lands" canary asserting the current sole Alembic head — both updated to `20260904_0038` (their own docstrings say to do exactly this), not a regression in either file.
- No frontend change — this package is backend-schema-only.

---

## 3. Test artifacts (per programme plan §15 "Tests," draft)

- `tests/test_organization_usage_events.py` (new — programme plan's own named file)
- `tests/test_organization_usage_api.py` (new — programme plan's own named file)
- `tests/test_contribution_reputation.py` (new — programme plan's own named file)
- `frontend/src/organizationUsage.test.js` (new — programme plan's own named file)
- `frontend/src/contributionReputation.test.js` (new — programme plan's own named file)

Programme plan line 932 states what these must prove, verbatim: "no raw query/private description/prompt/image/credential leakage, spoofed context rejection, aggregation thresholds and tenant isolation, idempotent governance events/reversals, bounded cardinality/retention, self-use exclusion, fair normalization/versioning, and no authorization dependence on score." This list is itself a strong signal for how adversarial WP9.9's audit needs to be, mirroring WP8.7's own cross-tenant rigor but with a privacy/PII axis Stage 8 did not need.

---

## 4. Decisions confirmed with Chris (2026-09-03, resumed session)

Unlike Stage 8's four decisions, these were genuine product/architecture choices this stage's own source documents left open — guessing wrong on any of them would have meant rebuilding schema or a whole service tier, not just relabeling a field. All nine are now resolved; §2's work-package sequence above has been finalized against these answers (see the per-package notes there).

1. **Q1 — Event catalog scope for v1: narrower core subset first.** WP9.1 builds only the browse-facing subset of Appendix B's catalog (session/context/preview/download/Favorite/set-selection) in its frozen `CheckConstraint` vocabulary; governance-lifecycle events (organization review, promotion, demotion, project/set lifecycle, icon, role/capability changes) are added in a follow-up extension to the same constraint once WP9.2 needs them, not invented ahead of need. Rationale: additive extension of a `CheckConstraint` is cheap; narrowing one after other code depends on it is expensive.
2. **Q2 — Points column: omitted entirely for v1.** `ContributionEvent` (WP9.5) will not have a `points` column at all until a real, versioned scoring policy is accepted separately (I-14/O1). No dead/unpopulated column, no premature commitment to the spec's illustrative §12.1 numbers. Adding the column later is its own small additive migration once a policy exists.
3. **Q3 — Badge set for v1: three well-defined badges only.** WP9.5 builds First Contribution, Contributor Organization, and Community Partner — each has an unambiguous trigger derivable from the `ContributionEvent` ledger alone. Multi-Discipline Contributor and Metadata Improver are deferred until Chris defines their exact thresholds (a discipline-count number; a precise definition of "metadata improvement" versus a new symbol) — building them now would mean inventing a heuristic, which `CLAUDE.md` prohibits.
4. **Q4 — Project/set lifecycle audit gap: closed in WP9.2.** WP9.2 adds both a governance `AuditEvent` and a product-usage event for project/set create/update/archive/select in the same transaction, since WP9.2 already touches these exact code paths and the gap (no audit trail today, unlike organization-review/promotion/demotion) was found as a direct byproduct of this stage's own research.
5. **Q5 — Browse-event ingestion: synchronous insert.** WP9.3 inserts `ProductUsageEvent` rows synchronously on the same request thread for preview/download/search/context events — no new async/worker infrastructure. Simplest and lowest-risk; accepted trade-off is minor added latency on browse-facing endpoints, not a "bounded durable" queue in the spec's most literal sense.
6. **Q6 — Dashboard rendering: hand-rolled stat tiles.** WP9.7 matches `OrganizationAdminPage.js`'s existing `dt`/`dd` stat-block convention — no new charting dependency, consistent with the existing visual language and `CLAUDE.md`'s validation-section bar on new npm dependencies.
7. **Q7/Q7b — Retention and aggregation threshold: confirmed numbers.** Raw `ProductUsageEvent`/`ContributionEvent` rows: 90-day retention, after which raw rows are purged; rolled-up aggregate counts are kept indefinitely for dashboards/trends. Minimum aggregation threshold: an Organization Admin or Platform Admin dashboard must suppress or group any aggregate cell backed by fewer than 3 distinct users, to prevent an "aggregate of one" from revealing an individual's behavior. Both numbers gate WP9.4's dashboard-query design and WP9.9's audit checks directly — WP9.1/WP9.4 must implement the purge job and the threshold enforcement to these exact numbers, not placeholders.
8. **Q8 — "Suspicious linked accounts" detection: scoped out of v1.** WP9.6 does not build this specific anti-gaming control — no Chris-confirmed detection signal exists to build against, and inventing one would violate `CLAUDE.md`'s prohibition on inventing workflow states/contracts. The other four §12.4 anti-gaming rules (no credit for uploads/likes/self-downloads/same-org-only, dedupe before review, hide points from reviewers — moot given Q2's no-points decision, so this reduces to "hide reputation detail from reviewers during review" — append-only + reversal corrections, rate-limiting) remain in WP9.6's scope. Revisit the suspicious-account control once a real signal is defined.
9. **Q9 — Opt-in leaderboard: deferred.** WP9.8 builds individual private stats and organization-only public attribution (badges/counts) but does not build the opt-in leaderboard in this stage — badges and stats alone already satisfy AC-22 and the spec's core dashboard requirements. The leaderboard is recorded as an explicitly out-of-scope nice-to-have for a later stage/patch, not silently dropped.

---

## 5. Regression standard (carried forward from Stage 5-8, expected to apply unchanged)

Per the Stage 5-8 precedent: any cross-tenant or cross-user data-visibility question in this stage's own domain (a different organization's usage data, another user's individual contribution/reputation detail) is exactly the class of bug that cannot be trusted to SQLite or to reasoning-from-the-code alone, and must be proven against the real disposable-Postgres harness this repository already uses for every prior stage's cross-tenant tests. This stage additionally introduces a privacy axis Stage 5-8 didn't need to prove as rigorously (individual-user behavioral data, not just organization-scoped governance state) — WP9.9's audit should treat "can an Organization Admin see another individual's raw behavior beyond an aggregate threshold" with the same rigor Stage 7/8 gave cross-tenant leaks.

---

## 6. Global prohibited side effects (applies to every package above, carried forward unchanged from Stage 8 §6)

No commit, staging, push, shared/real migration, deployment, service/gateway restart, feature activation, publication, withdrawal, external messaging, credential change, new npm dependency, or unrelated edit, unless a specific work package above and Chris's separate explicit go-ahead says otherwise. This explicitly includes: no migration applied to any shared database, and no flag flipped in any live environment, under any circumstance, without Chris's separate explicit approval for that exact action.
