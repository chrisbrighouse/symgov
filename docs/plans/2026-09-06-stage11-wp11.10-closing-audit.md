# Stage 11 WP11.10 — whole-stage closing audit

**Status:** Done, 2026-09-07. Mirrors the Stage 9 (WP9.9) and Stage 10
closing-audit format: independently re-run full regression, a Contract
Review against every acceptance criterion, a Security Review, and an
explicit deferred-vs-missed list.

## 1. Independent full regression re-run (2026-09-07, closing)

- Backend portable (`SYMGOV_BACKEND_TIMEOUT_SECONDS=1800 ./scripts/test-backend.sh`): **2298 passed, 3 skipped, 3 deselected, 6 failed in 808.53s.** The 6 failures are the same pre-existing baseline failures confirmed present on unmodified `origin/main` at WP11.1 (`test_auth_security_migration.py`, `test_frontend_forced_pin_change.py`, `test_llm_usage_migration.py`, `test_organization_migration.py`, `test_project_symbol_set_migration.py`, `test_project_symbol_set_postgresql.py::test_public_eligibility_rejects_revision_owned_by_a_different_symbol`) — none touch any file this stage changed.
- Backend external-workspace (`SYMGOV_EXTERNAL_TEST_TIMEOUT_SECONDS=300 ./scripts/test-backend.sh --external`): **clean** — Daisy (2 passed), DXF (7 passed), Libby duplicate triage (3 passed), Libby symbol vision (4 passed), Zip phase 2 (9 passed), published-review workflow (2 passed, 1 deselected), retired-Vlad-absence (1 passed, 5 deselected).
- `npm run test:frontend`: **250 passed**.
- `npm run build`: clean.
- Frozen compile gate (`backend/symgov_backend` + `scripts`): clean.
- `python3 scripts/secret_scan_added_lines.py --base origin/main` (WP11.2): clean.
- `git diff --check`: clean.
- `./scripts/test-langfuse-poc.sh` (the gap WP11.9 found and closed): **12 passed**.

## 2. Contract Review — acceptance-criterion traceability (programme plan §18)

| Criteria | Plan stage | Stage 11 disposition |
|---|---|---|
| AC-01–AC-03 personal/multi-organization sign-in and tenant rejection | Stages 1–2, 8 | Inherited, unchanged. Re-exercised structurally by WP11.3's personal/inactive-membership tests. |
| AC-04–AC-08 first/last organization admin, protected Symgov org, Platform Admin eligibility/continuity | Stages 1–3 | Inherited, unchanged. Re-exercised by WP11.7's bootstrap idempotency proof. |
| AC-09 generated/uploaded icon fallback | Stages 1, 3 | Inherited, unchanged. Icon upload remains disabled pending the malware-scan gap (deferred, §4 below). |
| AC-10–AC-11 all-member project selection and 50-character description | Stage 4 | Inherited, unchanged. |
| AC-12–AC-14 mixed sets, one active set, organization-wide inclusion | Stages 4–6 | **Directly widened by WP11.1** (organization-wide inclusion now also covers same-organization approved private items as direct set items) and re-verified for cross-tenant safety by WP11.1/WP11.3/WP11.6. |
| AC-15 explicit appointed-reviewer approval | Stage 5 | Inherited, unchanged. Re-exercised by WP11.3's `symbol_reviewer`-capability write-denial test (browse authority ≠ write authority, still admin-gated). |
| AC-16–AC-19 same-ID publication, company attribution, demotion, safe cross-tenant references | Stages 7–8 | Inherited, unchanged. |
| AC-20–AC-21 scoped advisory agents and prompt-injection resistance | Stage 10 | Inherited + structurally strengthened by WP11.5 (fail-closed/non-blocking proof: only `routes/agents.py` depends on either agent module). |
| AC-22 session/context/download telemetry | Stage 9 | Inherited, unchanged. |
| AC-23 American-English product UI | Stages 3–11, especially 8 | **Directly audited by WP11.5** for the Stage-11-new UI surfaces, with one real correction (the fabricated "Clear filters" claim, caught by WP11.6). |
| AC-24 online-only initial operation | Explicit exclusion + Stage 11 checks | **Directly confirmed**: nothing in WP11.1–WP11.9 introduces or implies any offline/local-first mode. Every new capability (Builder search filters, ETag guard, widened eligibility, the WP11.3 adversarial fixture, WP11.7's rehearsal) is a synchronous request/response HTTP or database operation with no client-side offline cache, service-worker, or queued-write mechanism added anywhere in the diff. |

## 3. Security Review

WP11.6 already performed an independent, adversarial security/code-quality
review of the exact release candidate through WP11.5 (a freshly-launched
agent with no memory of this session's own implementation, per programme
plan line 1003's "fresh immutable... review" requirement) — see
`docs/plans/2026-09-06-stage11-wp11.6-independent-review.md` for the full
report. That review is this stage's Security Review; it is not repeated
here. Its scope covered tenant-isolation/IDOR risk in the new eligibility
join, SQL injection, ReDoS, ETag/concurrency correctness, and the API-key
isolation claim (independently re-verified, and one of the review's own
claims was itself checked and found incorrect before being accepted —
see that document's Lane 2 finding 1). WP11.7 and WP11.8/11.9 (this
stage's remaining packages) added no new application code after WP11.6's
review — WP11.7 is test-only (against disposable, auto-torn-down
containers) and WP11.8/11.9 are documentation-only — so no further
application attack surface exists for a second security-review pass to
cover. The one code change made *after* WP11.6 (the corrected API-key
test names/framing and the EXPLAIN-capture rewrite in WP11.6's own
commit) was itself the product of that review and re-verified green
immediately after.

## 4. Deferred vs. missed

**Deliberately deferred (not fixed, not missed — reconfirmed as
still-intentional per WP11.9):**

- Icon-upload malware scan (fail-closed) not integrated — icon upload
  stays disabled in production (Stage 3, reconfirmed WP11.8/WP11.9).
- Icon abandoned-object reconciler not implemented (Stage 3).
- Reject/changes-requested decisioning for promotion requests —
  accept-only shipped (Stage 7).
- Cross-organization promotion-request-listing endpoint missing (Stage 7).
- `GET /published/packs` multi-symbol-pack partial-retirement accounting
  gap (Stage 7).
- Contribution points/scoring column does not exist yet (Stage 9).
- Several contribution badges and an opt-in leaderboard deferred (Stage 9).
- Usage-rollup refresh/purge batch jobs built but not wired to any
  scheduler (Stage 9, a separate ops/deployment decision).
- Three `AgentFinding.finding_type` slugs omitted from the v1
  `CheckConstraint` (Stage 10).
- No retention/purge policy for `agent_findings` (Stage 10).
- No live Hermes agent identity/binding for Organization Steward/Platform
  Governance — handled outside this repository's commits, per the
  kickoff prompt; Stage 11 verified the repo-side behavior only
  (WP11.5/WP11.9).
- **New in Stage 11, deliberately left open per the plan's own standing
  decision (not a gap, a scheduled future decision):** WP11.8's pilot-
  expansion targets/timing beyond the mandatory `symgov`-only first step.
- **New in Stage 11, deliberately deferred (WP11.5):** no server-side row
  cap on the organization-private half of Builder search (predates
  WP11.1; recorded as a known scalability item for a future stage).

**Found and fixed during Stage 11 (not left as missed):**

- Three evidence-integrity defects caught by WP11.6's independent review
  (a fabricated UI-copy claim, misleadingly-framed API-key tests, a
  drift-prone performance assertion) — all corrected in WP11.6's own commit.
- The Langfuse gate gap found and closed while writing WP11.9.

**Nothing else was found missed** during this closing audit — every item
in the WP11.9 walkthrough closed with real evidence or an explicit,
already-tracked deferral; this closing regression re-run and Contract
Review surfaced no new gap beyond what WP11.6/WP11.9 already recorded.

## 5. Outcome

Stage 11's own scope — integrated hardening, migration rehearsal, and a
drafted (not executed) controlled-release package — is complete. Per
programme plan §17's own text, this stage produces production-ready
source and an explicit, reversible rollout package; **it does not itself
authorize deployment.** Nothing in WP11.1–WP11.10 has been pushed to
`origin/main`, and no migration, deployment, publication, withdrawal,
gateway, or service action has been taken against any real or shared
environment. Any further action — pushing this branch, merging it,
executing any step of WP11.8's rollout plan — requires Chris's separate,
explicit authorization for that specific action.
