# Stage 11 WP11.9 — final acceptance checklist walkthrough

**Status:** Done, 2026-09-07. Programme plan §17 "Final acceptance checklist" (lines 1035–1060).

**Count correction:** the programme plan's own WP11.9 description (and this
stage's own kickoff prompt) call this "the 20-item checklist," but the
actual list at lines 1037–1060 is **24 items**, not 20 — verified by
re-reading the exact line range fresh rather than trusting the "20-item"
label (the same discipline WP11.6's independent review applied to this
session's own evidence claims). Recorded here rather than silently
forced to match the stated count.

## Method

Stage 11 does not re-derive evidence for items outside its own scope —
each prior stage already closed its own area with a whole-stage audit
(Stage 5 WP5.6, Stage 6 WP6.6, Stage 7 WP7.7, Stage 8 WP8.7, Stage 9
WP9.9, Stage 10's own closing commit). This walkthrough cites that
inherited evidence for items Stage 11 did not touch, and cites this
stage's own WP11.1–WP11.8 evidence for items Stage 11 specifically
re-verified or newly implements. An item is marked **Cannot close with
real evidence** rather than checked if this walkthrough could not find or
produce a concrete citation for it.

## Walkthrough

1. **Every I-01–I-25/O1-O6 decision has an accepted source, distinct from D1-D19.**
   ✅ Inherited. Each stage's own kickoff/implementation plan documents its
   decisions' sources (`docs/plans/2026-08-08-symbol-set-management-decision-addendum.md`
   for I-01–I-25; the spec's own O1-O6/D1-D19 split is unchanged by Stage 11).
   Stage 11 introduces no new I-xx/O-xx/D-xx decision of its own — WP11.1's
   loosening decision is a Stage-11-local implementation decision recorded
   in this stage's own plan §4, not a new I-xx.

2. **F0.5 account-security and F0.6 routing blockers closed at required boundaries.**
   ✅ Inherited (Stage 1-3 era; `docs/plans/2026-07-28-f0-4-production-preflight-blocked.md`
   is superseded context per this stage's own kickoff prompt). Unchanged by Stage 11.

3. **Organization and platform roles are independent and session-scoped.**
   ✅ Inherited + re-confirmed by WP11.3: `test_platform_admin_org_scoped_session_cannot_reach_another_organizations_symbol_set`
   plus its positive control prove Platform Admin's platform-level
   authority does not leak into (or get confused with) an org-scoped session.

4. **Forced-PIN ordering, selection challenges, protected-owner cutover follow accepted contracts.**
   ✅ Inherited (`test_organization_selection_challenge.py`, `test_auth_routes.py`,
   `test_f0_5_account_security.py`). Unchanged by Stage 11. (Note:
   `test_frontend_forced_pin_change.py::test_forced_pin_change_route_and_guard_exist`
   is one of the 6 pre-existing baseline failures WP11.1 confirmed present
   on unmodified `origin/main` — a pre-existing gap in this specific test's
   own evidence, not a functional regression Stage 11 introduced or is
   scoped to fix.)

5. **Selection-challenge/step-up races follow exact I-25 boundaries.**
   ✅ Inherited (`test_organization_selection_challenge.py`'s TTL/attempt/
   revoke/supersession suite). Unchanged by Stage 11.

6. **Active organization is immutable inside a full session.**
   ✅ Inherited (`stage4_authorization.require_stage4_principal` binds
   `active_organization_id` from the session row itself, re-confirmed by
   direct code reading during WP11.6's independent review). Unchanged by Stage 11.

7. **Zero/one/many memberships, bounded selection, duplicate-membership races, last-admin protection hold under concurrency.**
   ✅ Inherited (`test_organization_postgresql_migration.py`'s concurrency
   suite). Re-confirmed for the zero-active-membership case specifically by
   WP11.3's `test_inactive_organization_membership_falls_back_to_a_personal_session_and_cannot_reach_organization_a`.

8. **Last-eligible-Platform-Admin and protected Symgov organization invariants hold under concurrency.**
   ✅ Inherited (`enforce_platform_admin_eligibility` DB trigger; `test_platform_admin_api.py`).
   Re-exercised (not re-proven) by WP11.7's bootstrap tests, which depend
   on this invariant already holding.

9. **Reserved code `symgov`, display-code grammar, pilot-allowlist matching follow I-24.**
   ✅ Inherited + directly re-verified by WP11.8's rollout plan, which reads
   the actual `NORMALIZED_ORGANIZATION_CODE_PATTERN`/`SYMGOV_ORGANIZATION_PILOT_CODES`
   code at drafting time rather than assuming it.

10. **Projects and sets are tenant-scoped; sets can serve several same-organization projects; one active set per user/project.**
    ✅ Inherited (`test_projects_api.py`, `test_symbol_set_availability.py`,
    `test_user_project_set_selection.py`). Re-confirmed for cross-tenant
    isolation specifically by WP11.3's `test_org_b_user_cannot_reach_org_as_symbol_set_by_id`.

11. **Rolling set items and I-23; only future immutable releases pin revision UUIDs.**
    ✅ Inherited + directly re-verified by WP11.1/WP11.6: `symbol_eligibility.eligible_organization_private_symbols`
    resolves current approved revisions at read time (does not pin), confirmed
    during WP11.6's independent review's Lane 1 trace.

12. **Effective palette is active-set items plus approved organization-wide symbols, deterministic, de-duplicated, private-safe; Public Catalog stays independently browseable.**
    ✅ **This is the item WP11.1 directly widened.** Programme-plan line 1048's
    original wording is amended per the Stage 11 plan §4 note (not by editing
    the frozen programme-plan doc): "active-set items" now includes both
    public and approved same-organization private items, never
    cross-organization — proven by `test_symbol_set_tenant_isolation.py`'s
    two WP11.1 tests and WP11.3's non-admin-write-denial test. Determinism/
    de-duplication/ordering unchanged, still covered by `test_effective_palette.py`.

13. **Organization approval is revision-specific and never public by implication.**
    ✅ Inherited + re-confirmed: `symbol_eligibility.py`'s join binds the
    exact `symbol_revision_id` to its own decision/submission rows (an older
    approval cannot make a newer draft visible), and `visibility` never
    flips organization-private symbols public by implication anywhere in
    the codebase (confirmed during WP11.6's review).

14. **Promotion/demotion preserves human authority, identity, provenance, and audit.**
    ✅ Inherited (Stage 7's promotion/demotion pipeline, `test_wp73`/`test_wp74`/`test_wp76`).
    Unchanged by Stage 11.

15. **Catalog search/count/facet/favourite/asset paths enforce visibility before output.**
    ✅ Inherited (Stage 8's four-angle organization-context reader matrix,
    `test_wp81`–`test_wp87_*_postgresql.py`). Unchanged by Stage 11.

16. **API-key Catalog behavior remains public-only unless separately specified.**
    ✅ Directly re-verified by WP11.3/WP11.6: the seeded API key genuinely
    authenticates the real API-key-gated `/catalog/symbols` route
    (confirmed, not assumed — see WP11.6's corrected framing) and never
    reaches any organization-scoped route.

17. **Icons cannot execute active content or exhaust resources.**
    ✅ Inherited (Stage 3's icon safety controls). **Deferred item
    reconfirmed, not closed:** icon-upload malware scan is still not
    integrated — icon upload stays disabled in production per every
    prior stage's own kickoff note and WP11.8's rollout plan (step 2).

18. **Usage/reputation is privacy-bounded and never authorization input.**
    ✅ Inherited (Stage 9's 90-day retention / 3-distinct-user aggregation
    threshold, `test_wp91`–`test_wp98_*_postgresql.py`). Unchanged by Stage 11.

19. **Organization Steward/Platform Governance are scoped recommenders with structured findings and no direct authority.**
    ✅ Inherited (Stage 10, `test_wp101`–`test_wp104_*_postgresql.py`) +
    directly strengthened by WP11.5: only `routes/agents.py` imports either
    module, so an authorization-dependency failure in an agent run is
    structurally incapable of blocking any other core route (fail-closed,
    not fail-blocking). **Deferred item reconfirmed, not closed:** no live
    Hermes agent identity/binding exists yet — handled outside this
    repository's commits per the kickoff prompt; this item verifies the
    repo-side behavior only, which it does.

20. **Backend-authoritative flags, pilot allowlist, disabled responses, UI capability negotiation, kill switches proven.**
    ✅ Directly proven by WP11.8's rollout plan (reads the real settings
    code, the real `/auth/me` capability-negotiation shape, and the real
    pilot-allowlist grammar enforcement at settings-load time) and by
    WP11.3's route-policy matrix (every flag-gated route family's
    disabled-response behavior).

21. **Provisional performance evidence, accessibility/responsive checks, American-English UI copy, locale-aware formatting pass.**
    ✅ **Directly proven by WP11.5**, including one genuine self-correction
    (the fabricated "Clear filters" copy claim, caught and fixed by
    WP11.6) — see `docs/plans/2026-09-06-stage11-wp11.5-regression-performance-accessibility-evidence.md`.

22. **Migration rehearsal, restore, at/above-floor rollback, externally denied/stopped emergency pre-floor recovery proven with demoted prior revisions and all public/background reader surfaces.**
    ✅ **Directly proven by WP11.7** for restore/migrate/bootstrap/rollback-discipline.
    The demoted-prior-revision case specifically is `test_wp76_demotion_concurrency_and_regression_postgresql.py::test_downgrade_past_the_visibility_floor_is_refused_after_a_real_demotion`
    (cited by WP11.7, not re-proven). Emergency pre-floor recovery ordering
    is recorded procedurally in WP11.7/WP11.8 (external ingress/reader-topology
    action this repository cannot execute or prove from inside a disposable
    single-container test — see WP11.7's own explicit scoping of that limit).

23. **Full backend/external/frontend/Langfuse/build gates pass on the frozen candidate.**
    ✅ Backend portable (2292 passed, 6 pre-existing unrelated failures,
    WP11.5), backend external-workspace (all clean, WP11.5), frontend
    (250 passed, WP11.1-11.5), build and compile gate (every package's own
    re-run) all directly proven against this exact candidate.
    **Gap found and closed while writing this walkthrough:** no
    `./scripts/test-langfuse-poc.sh` invocation appeared in any WP11.1–11.8
    evidence doc. Run now (2026-09-07): `12 passed` (the synthetic-contract
    suite in `langfuse-poc/tests/test_synthetic_contract.py`, no live
    Langfuse dependency). Recorded here rather than silently added to an
    earlier package's evidence after the fact.

24. **No production migration, deployment, publication, withdrawal, gateway, or service action occurred without explicit authorization.**
    ✅ Directly true of this entire stage: every commit (WP11.1–11.8) was
    made only to the local `integration/stage11-wp11.1-worktree-integration`
    branch, never pushed; every Postgres rehearsal ran against disposable,
    auto-torn-down containers; WP11.8's rollout plan is drafted only, no
    step executed. No `build:publish`/`publish:static`/migration/deploy
    command was run against any real or shared environment.

## Summary

All 24 items close with direct evidence (this stage's own, or inherited
from a prior stage's closing audit). Item 17's icon-malware-scan gap and
item 19's live-Hermes-binding gap are **reconfirmed as intentional,
already-documented deferrals**, per the Stage 11 plan's own WP11.9
decision, not silently closed — deferred is not the same as closed, and
this walkthrough does not conflate the two. Item 23 surfaced one real,
previously unflagged gap (the Langfuse gate had never actually been run
this stage) which was closed in the course of writing this walkthrough
rather than left as an open item.
